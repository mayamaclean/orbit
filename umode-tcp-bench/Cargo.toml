[package]
name = "umode-tcp-bench"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "umode-tcp-bench"
path = "src/main.rs"

[dependencies]
net_channel = { path = "../net_channel", default-features = false }
orbit-abi   = { path = "../orbit-abi" }
orbit-rt    = { path = "../orbit-rt" }
