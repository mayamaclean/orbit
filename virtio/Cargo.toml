[package]
name = "virtio"
version = "0.1.0"
edition = "2024"

[dependencies]
dtoolkit = { version = "0.1.1", default-features = false }
tracing = { version = "0.1.44", default-features = false, features = [] }
