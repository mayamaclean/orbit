[package]
name = "hello-fb-std"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "hello-fb-std"
path = "src/main.rs"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
ab_glyph = "0.2.32"
orbit-abi = { path = "../orbit-abi" }
orbit-text = { path = "../orbit-text" }
