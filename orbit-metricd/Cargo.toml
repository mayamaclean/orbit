[package]
name = "orbit-metricd"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "orbit-metricd"
path = "src/main.rs"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
orbit-abi = { path = "../orbit-abi" }
