[package]
name = "mmu"
version = "0.1.0"
edition = "2024"

[dependencies]
mem = { path = "../mem" }
riscv = "0.16.0"
tracing = { version = "0.1.44", default-features = false, features = [] }

[features]
alloc = ["mem/alloc"]